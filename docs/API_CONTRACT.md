# API contract

<!-- Generated by `npm run docs:api --workspace @isd/api`. Do not edit by hand. -->

Base path `/api/v1` · 59 endpoints · generated from the route metadata,
so it cannot drift from the code.

Routes marked 🔒 require an authenticated admin session.

## Response envelope

Every response has the same shape (PROJECT_PLAN.md §8):

```jsonc
{ "success": true,  "data": {}, "meta": {} }
{ "success": false, "error": { "code": "VALIDATION_ERROR", "message": "…", "details": [] } }
```

Listing endpoints add pagination and facet counts to `meta`:

```jsonc
"meta": {
  "page": 1, "limit": 24, "total": 1049, "totalPages": 44,
  "facets": { "categories": [...], "brands": [...], "industries": [...] }
}
```

## Authentication

Admin routes require a JWT in an httpOnly cookie (`isd_at`) from
`POST /admin/auth/login`. A bearer token is accepted as a secondary source
for tooling. Refresh rotates the token and invalidates the previous one.

An account with `mustChangePassword` set can reach only `/admin/auth/me`,
`/admin/auth/change-password` and `/admin/auth/logout` until it is cleared.

## There are no prices

This is a quotation-only platform. No endpoint accepts or returns a price,
and none should be added — see CLAUDE.md §1.

## Endpoints

### Admin — uploads

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `POST` | `/admin/uploads/signature` 🔒 | Signed params for a direct-to-Cloudinary upload |

### Admin — auth

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `POST` | `/admin/auth/login` 🔒 | Sign in and receive httpOnly auth cookies |
| `POST` | `/admin/auth/refresh` 🔒 | Rotate the refresh token and reissue the pair |
| `POST` | `/admin/auth/logout` 🔒 | Invalidate the refresh token and clear cookies |
| `GET` | `/admin/auth/me` 🔒 | Current admin profile |
| `POST` | `/admin/auth/change-password` 🔒 | Change password (required on first sign-in) |

### System

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/health` | Liveness probe for UptimeRobot |

### Catalogue — categories

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/categories` | Full category tree, or a flat list with ?flat=true |
| `GET` | `/categories/menu` | Mega-menu tree, trimmed to what the panel renders |
| `GET` | `/categories/{slug}` | Single category with its breadcrumb trail |

### Admin — categories

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/admin/categories` 🔒 | Full tree including inactive categories |
| `POST` | `/admin/categories` 🔒 | Create a category |
| `PATCH` | `/admin/categories/reorder` 🔒 | Bulk displayOrder update |
| `GET` | `/admin/categories/{id}` 🔒 | Single category for the edit form |
| `PATCH` | `/admin/categories/{id}` 🔒 | Update a category |
| `DELETE` | `/admin/categories/{id}` 🔒 | Soft delete — blocked when children or products remain |

### Catalogue — brands

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/brands` | Active brands, ordered for the brand strip |
| `GET` | `/brands/{slug}` | Single brand |

### Admin — brands

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/admin/brands` 🔒 | All brands including inactive |
| `POST` | `/admin/brands` 🔒 |  |
| `GET` | `/admin/brands/{id}` 🔒 |  |
| `PATCH` | `/admin/brands/{id}` 🔒 |  |
| `DELETE` | `/admin/brands/{id}` 🔒 | Soft delete — blocked while products reference it |

### Catalogue — industries

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/industries` | Active industries |
| `GET` | `/industries/{slug}` | Single industry with its long-form content |

### Admin — industries

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/admin/industries` 🔒 |  |
| `POST` | `/admin/industries` 🔒 |  |
| `GET` | `/admin/industries/{id}` 🔒 |  |
| `PATCH` | `/admin/industries/{id}` 🔒 |  |
| `DELETE` | `/admin/industries/{id}` 🔒 | Soft delete — blocked while products reference it |

### Catalogue — products

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/products` | Product listing with facet counts |
| `GET` | `/products/slugs` | All active slugs, for generateStaticParams and the sitemap |
| `GET` | `/products/{slug}` | Single product with category, brand and industries resolved |
| `GET` | `/products/{slug}/related` | Eight products from the same category |

### Admin — products

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/admin/products` 🔒 | Paginated product list, filterable by category, brand and status |
| `POST` | `/admin/products` 🔒 |  |
| `GET` | `/admin/products/{id}` 🔒 | Full record for the edit form |
| `PATCH` | `/admin/products/{id}` 🔒 |  |
| `DELETE` | `/admin/products/{id}` 🔒 | Soft delete — Cloudinary assets are kept, the action is reversible |
| `PATCH` | `/admin/products/{id}/images/reorder` 🔒 | Reorder images; index 0 becomes the primary |
| `DELETE` | `/admin/products/{id}/images/{publicId}` 🔒 | Remove an image and destroy it in Cloudinary — permanent |

### Catalogue — search

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/search/suggest` | Typeahead suggestions, max 8 |

### Quotations

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `POST` | `/quotations` | Submit a quotation request |

### Admin — quotations

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/admin/quotations` 🔒 | Paginated quotations, filterable by status, date range and search |
| `GET` | `/admin/quotations/export` 🔒 | CSV of the filtered set |
| `GET` | `/admin/quotations/{id}` 🔒 | Full quotation for the detail modal |
| `DELETE` | `/admin/quotations/{id}` 🔒 | Soft delete — the record is kept and can be restored |
| `PATCH` | `/admin/quotations/{id}/status` 🔒 | Change status; appends to statusHistory |
| `POST` | `/admin/quotations/{id}/notes` 🔒 | Add an internal note |

### Catalogue download

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/catalogue/active` | Active catalogue metadata — no file URL when gated |
| `POST` | `/catalogue/download` | Submit the lead form and receive a time-limited download URL |

### Admin — catalogue

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/admin/catalogue` 🔒 | All catalogue files, active first |
| `POST` | `/admin/catalogue` 🔒 | Register an uploaded catalogue PDF |
| `GET` | `/admin/catalogue/leads/export` 🔒 | CSV of the filtered leads |
| `GET` | `/admin/catalogue/leads` 🔒 | Paginated download leads |
| `PATCH` | `/admin/catalogue/{id}/activate` 🔒 | Make this file active and deactivate the rest |
| `PATCH` | `/admin/catalogue/{id}` 🔒 | Update metadata; the PDF itself is replaced by a new record |
| `DELETE` | `/admin/catalogue/{id}` 🔒 | Soft delete — the Cloudinary asset is kept |

### Admin — dashboard

| Method | Path | Purpose |
| ------ | ---- | ------- |
| `GET` | `/admin/dashboard/stats` 🔒 | Counts and recent activity for the admin dashboard |

## Interactive docs

Swagger UI runs at `/api/v1/docs` outside production. It is off in
production by default because it enumerates every route and validation rule
(§3.3); set `SWAGGER_ENABLED=true` to serve it anyway.

The machine-readable spec is `docs/openapi.json` — import it into Postman,
Insomnia or an OpenAPI client generator.
